apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-deployment
  namespace: dev
  labels:
    app: mobile
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mobile
  template:
    metadata:
      labels:
        app: mobile
    spec:
      containers:
      - name: mobile
        image: 339712867708.dkr.ecr.ap-south-1.amazonaws.com/ecr_repo:21
        ports:
          - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: mobile-srv
  namespace: dev   
spec:
  type: ClusterIP
  selector:
    app: mobile           
  ports:
    - protocol: TCP
      port: 8080                      
      targetPort: 80
---      
